Manual SQL patches used during live verification and post-schema alignment.

Keep permanent schema changes in `supabase/migrations/`.
Keep one-off recovery, hotfix, or production support scripts in this folder.
